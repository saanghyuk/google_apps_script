function myFunction() {
  var x = 'Hello';
  var z = 10;
  var y = false;
  var array = ["Hello", 10, false]
  array[0];
  var obj = { 'first': 'Laurence', 'last': 'Svekis' };
  obj.first;
  obj['first'];
  myFunc1("World")

  for (var x = 0; x < 10; x++) {
    //code
    Logger.log(x)
  }

  Logger.log(x)

  if (x == 5) {
    Logger.log('FIVE')
  } else {
    Logger.log("ELSE")
    Logger.log(x)
  }
}

function myFunc1(world) {
  Logger.log(world)
  var a = 5;
  var b = 10;
  var r = 'World';
}
